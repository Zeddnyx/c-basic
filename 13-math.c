#include <stdio.h>
#include <math.h>

int main() {

  double A = sqrt(9); // 3
  double B = pow(2, 3); // 2^3
  int C = round(3.14); // rounds up (4)
  int D = ceil(3.14); // rounds up (4)
  int E = floor(3.99); // rounds down (3)
  double F = fabs(-3.14);  // put negative number (3.14)
  double G = log(3); // log base e (1.0986)
  double H =  sin(34); 
  double I =  cos(34);
  double J =  tan(34);
  
  printf("%lf",I);
  return 0;
}
